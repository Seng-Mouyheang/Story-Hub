VITE_API_PROXY_TARGET=http://localhost:3001
# Set this only when the frontend and backend deploy to different origins.
# Leave blank when a reverse proxy serves /api from the same production domain.
VITE_API_BASE_URL=
